generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Product Table
model Product {
  id        Int    @id @default(autoincrement())
  name      String
  price     Int
  stock     Int
}